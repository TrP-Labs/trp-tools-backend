model User {
    id          String @id @default(uuid())
    robloxId    Int @unique
    siteRank    String

    sessions    Session[]
    apiKeys     Key[]

    favoriteRoutes   Route[] @relation("FavoriteRoutes")
    dislikedRoutes    Route[] @relation("DislikedRoutes")
}